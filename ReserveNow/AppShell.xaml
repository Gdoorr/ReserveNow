<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:ReserveNow"
       x:Class="ReserveNow.AppShell">
    <Shell.RouteTable>
        <Route Uri="LoginPage" Page="{x:Type views:LoginPage}" />
        <Route Uri="MainPage" Page="{x:Type views:MainPage}" />
    </Shell.RouteTable>
</Shell>
